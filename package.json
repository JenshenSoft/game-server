{
  "name": "game-server",
  "version": "0.0.0",
  "private": true,
  "description": "Server for card games",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test"
  },
  "license": "MIT",
  "devDependencies": {
    "lerna": "^2.11.0"
  },
  "engines": {
    "node": ">=9.0.0",
    "npm": ">=5.0.0"
  }
}
